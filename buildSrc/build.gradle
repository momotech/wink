apply plugin: 'java-gradle-plugin'
apply plugin: 'kotlin'
apply plugin: 'maven-publish'

allprojects {
    repositories {
        google()
        jcenter()
    }
}

buildscript {
    ext {
        gradleVersion = '7.2.2'
        kotlinVersion = '1.6.10'
    }
    repositories {
        google()
        jcenter()
    }

    dependencies {
        classpath "org.jetbrains.kotlin:kotlin-gradle-plugin:$kotlinVersion"
    }
}

repositories {
    google()
    mavenLocal()
    mavenCentral()
}

task sourceJar(type: Jar) {
    classifier = 'sources'
    from sourceSets.main.allSource
}
artifacts {
    archives sourceJar
}

dependencies {
    runtimeOnly subprojects.collect { project(it.path) }
}